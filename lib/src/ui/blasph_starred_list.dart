import 'package:flutter/material.dart';

class BlasphStarredList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("Starred List")
    );
  }

}